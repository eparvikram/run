apiVersion: v1
kind: Service
metadata:
  name: my-fastapi-app-service
spec:
  selector:
    app: my-fastapi-app
  ports:
    - protocol: TCP
      port: 80 # Service exposed port
      targetPort: 8000 # Container's port
  type: NodePort # This might be changed to LoadBalancer in prod overlay